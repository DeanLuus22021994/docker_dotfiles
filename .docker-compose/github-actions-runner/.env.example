# GitHub repository URL for docker-examples
GITHUB_URL=https://github.com/DeanLuus22021994/docker-examples

# Runner token (get this from: https://github.com/DeanLuus22021994/docker-examples/settings/actions/runners/new)
# Note: Tokens expire after 1 hour, so generate a new one when setting up
RUNNER_TOKEN=BOVDJEANEEFAVOEQP5TKKCTI642HQ

# Runner name (optional, defaults to docker-examples-runner)
RUNNER_NAME=docker-examples-runner

# Work directory (optional, defaults to /actions-runner/_work)
RUNNER_WORKDIR=/actions-runner/_work

# Labels for the runner (optional, defaults to self-hosted,linux,x64)
LABELS=self-hosted,linux,x64,docker,examples

# Runner group (optional, defaults to Default)
RUNNER_GROUP=Default
