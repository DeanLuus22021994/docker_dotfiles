{
  "name": "docker-api-proxy",
  "version": "1.0.0",
  "description": "Docker Engine API proxy for cluster dashboard",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "docker",
    "api",
    "proxy",
    "metrics"
  ],
  "author": "Cluster Dashboard Team",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "dockerode": "^4.0.2",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
